export enum Stat {
    PlayerLevel, // klass
    ItemsFound, // (klass, zone,)
    SpellsCast, // (klass,)
    Clicks, // (klass)
    SkillLevel, // skill (basically obligatory)
    ActionsTaken, // zone
    MAX
}
